# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.9.96] - 2026-08-18

首个面向开源发布的版本。原名 `HashValidatorPlus`，已更名为 `ErgeHash`（中文名：二哈）。

### 新增

- **多算法哈希计算**：支持 MD5 / SHA-1 / SHA-256 / SHA-512 / CRC32 等主流算法，单文件可同时计算多种算法
- **校验文件导入验证**：支持 `.md5` / `.sfv` / `.sha256` / `.sha512` 等多种格式自动嗅探、自愈解析与逐文件绑定校验
- **批量生成标准校验文件**：按所选算法生成校验文件并写入源文件同目录
- **预期哈希比对**：支持手动输入预期哈希集合进行匹配校验，自动规范化分隔符与算法名前缀
- **独立报告窗口**：在独立窗口展示完整校验报告，支持单条复制、右键多文件累积对比
- **快速指南 / 设置 / 关于 页面**：以 React 组件实现并接入国际化（中文 / 英文）
- **全局快捷键体系**：统一、单一数据源的快捷键管理
- **主题系统**：基于 CSS 变量的浅色 / 深色主题，跟随品牌色
- **拖放支持**：直接拖入文件或文件夹进行计算
- **国际化**：内置中文与英文
- **彩蛋页**：展示作者 B 站 / 微信等联系方式

### 变更

- **批量校验性能优化**：改为多算法单趟读取并节流进度事件，大幅提升大文件校验速度
- 提升构建优化级别，修复哈希计算性能倒退
- 快速指南由静态 HTML 改为 React 组件，对齐设计系统风格
- 菜单项 hover 统一为品牌色，logo 文字颜色改为品牌色

### 修复

- 报告窗口关闭按钮点击无响应及启动白闪
- 批量校验进度条不刷新
- 设置页 Switch 圆点溢出胶囊边界
- 预期哈希分隔符处理（保留中文句号、收敛分隔符为空格清除）
- 窗口拖放失效

### 移除

- 废弃的 `MenuBar` 组件（已由 `TitleBar` 取代）
- 清理未引用的遗留组件与冗余依赖（`dirs`、`svgo` 等），精简打包体积
- 移除 `bundle.targets` 限制，恢复多平台（Windows / macOS）默认打包

[Unreleased]: https://github.com/ErgeAIA/ErgeHash/compare/v0.9.96...HEAD
[0.9.96]: https://github.com/ErgeAIA/ErgeHash/releases/tag/v0.9.96
