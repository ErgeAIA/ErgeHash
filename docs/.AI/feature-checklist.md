# 功能对照清单：PyQt 原版 vs Tauri 现状

> 依据 `docs/PyQt_to_Tauri2_迁移指令.md` 阶段五「功能验证」需要，从 PyQt 原代码（`src/`，main.py 2458 行 + 5 模块）逐功能提取，与 `tauri-app/` 当前实现逐项核对。作为阶段 5 验收底稿。状态：`✅ 可用` / `⚠️ 部分/待验证` / `❌ 缺失或损坏`。

> **实施状态（2026-08-10）**：清单全部缺口已在 P0-P3 处理（契约统一、缓存键、暂停/批量取消、导出/导入/历史/批量文件夹/记事本/拖放、配置持久化、快速比较语义、复制格式、清空确认）。剩余需桌面验证项：拖放、暂停/恢复运行期效果、导出/导入/历史/记事本交互、窗口几何。

## 文件与列表

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 1 | 添加文件（多选对话框） | ✅ QFileDialog 多选 | ✅ `openFileDialog` + `addFiles` | |
| 2 | 添加文件夹（递归扫描） | ✅ 选目录 + rglob | ✅ `openFolderDialog` + `scanDirectory` | |
| 3 | 拖放文件/文件夹 | ✅ QDragDrop + 递归 | ⚠️ 依赖 `File.path` | Tauri 2 无此属性（HTML5 DnD），疑失效，**待真机验证** |
| 4 | 清空列表 | ✅ 带确认 | ✅ 无确认 | 行为差异，可接受 |
| 5 | 右键菜单 | ✅ 删除/清空/复制路径 | ✅ 删除/复制路径/复制哈希 | 无「清空列表」项；多「复制哈希」 |
| 6 | 列表去重 | ✅ 已存在跳过 | ✅ addFiles 去重 | |
| 7 | 文件状态着色（成功/错误/不匹配） | ✅ 绿/橙/红 | ✅ success/mismatch/error | |

## 哈希计算

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 8 | 算法选择 SHA-256/MD5/SHA-1/SHA-512 | ✅ 单选默认 sha256 | ✅ Sidebar RadioGroup | |
| 9 | 批量校验 | ✅ 队列顺序处理 | ✅ `start_batch_validation` | 事件字段不匹配见 BUG-002 |
| 10 | 分块读取 + 实时进度 | ✅ 81KB 块 | ✅ 8KB 块 + `hash-progress` 事件 | 进度事件字段 `file_path` vs 前端 `filePath` 不匹配 |
| 11 | 空文件处理 | ✅ 直接返回空哈希 | ✅ 正常 | |
| 12 | 哈希缓存 | ✅ `(size, algo)` | ✅ 同款 | **正确性隐患见 BUG-004** |
| 13 | 暂停 / 继续 | ✅ threading.Event | ❌ **无效** | 后端 `pause_flag` 从不轮询，见 BUG-001 |
| 14 | 取消 | ✅ should_stop | ⚠️ 单文件循环检查；批量内层不检查 | 大文件批量时取消不响应 |
| 15 | 错误提示（不存在/权限/访问） | ✅ 分类文案 | ✅ Err 字符串 | |

## 哈希验证

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 16 | 比较哈希值（单行/多行） | ✅ 单行比第一个；多行逐行 | ✅ 单行比全部；多行逐行 | 单行语义略有差异 |
| 17 | 格式校验（hex） | ✅ | ✅ | |
| 18 | 快速比较两个文件 | ✅ >1GB 只读前 5MB，≤1GB 全读 | ⚠️ 固定只读前 10MB | **行为差异**：小文件也只读前缀，可能误判 |
| 19 | 批量处理文件夹（Ctrl+B） | ✅ 确认 + 结果弹窗 + 询问导出 | ❌ **损坏**：把文件夹路径当文件加入列表 | 需改为扫描目录后加入文件列表 |

## 结果与操作

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 20 | 结果展示 | ✅ HTML（✓/✗ + 哈希 + 耗时 + 缓存标记） | ✅ 纯文本 textarea | 样式差异可接受 |
| 21 | 批量完成统计 | ✅ 总数/成功/错误/总耗时 | ⚠️ 字段不匹配 | `payload.errors` / `payload.totalTime` 为 undefined（BUG-002） |
| 22 | 复制结果 | ✅ 仅哈希行（批量=文件名:哈希多行） | ⚠️ 复制整个 resultText | 含提示文字，行为差异 |
| 23 | 导出 CSV / JSON / 验证文件 | ✅ 菜单 + 保存对话框 | ❌ **前端未接线** | `export-results` 事件无监听，无导出按钮 |
| 24 | 导入验证文件 | ✅ 工具菜单 | ❌ **前端未接线** | `import-verification` 事件无监听 |

## 历史与配置

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 25 | 历史记录查看 / 使用 | ✅ 双击或「使用选中项」 | ✅ HistoryDialog | 填充预期哈希 + 算法 |
| 26 | 历史记录写入 | ✅ 计算后自动追加（50 条去重） | ❌ **addHistory 从未被调用** | 历史永远为空 |
| 27 | 清空历史 | ✅ 带确认 | ⚠️ 有（dispatch 已接线）无确认 | |
| 28 | 主题暗/亮切换 | ✅ 持久化 | ⚠️ 切换生效，**不持久化** | 无 `set_config` 调用 |
| 29 | 语言 zh/en 切换 | ✅ 持久化 | ⚠️ 切换生效，**不持久化** | |
| 30 | 算法 / 窗口几何持久化 | ✅ | ❌ 均不写配置 | |
| 31 | 配置存储位置 | `~/.hashvalidatorplus/` | app data 目录 | 存储位置合理 |

## 界面与系统

| # | 功能 | PyQt 原版 | Tauri 现状 | 备注 |
|---|------|-----------|-----------|------|
| 32 | 侧边栏：记事本 | ✅ 打开系统 notepad | ❌ **空实现** | onClick 为空 |
| 33 | 侧边栏：历史 / 设置 | ✅ | ✅ | |
| 34 | bilibili 链接 | ✅ | ✅ | Settings 里 GitHub 为占位假链接 |
| 35 | 菜单栏（文件/编辑/工具/指南） | ✅ | ✅ 结构在 | 导出/导入项点击无效（见上） |
| 36 | 快捷键 Ctrl+O / Ctrl+B / Ctrl+Q | ✅ | ✅ | 另有 Ctrl+C 复制哈希 PyQt 有、Tauri 无 |
| 37 | 状态栏 + 实时时间 | ✅ | ✅ | |
| 38 | 快速指南 | ✅ quick_start.html + 暗色适配 | ✅ fetch `/quick_start.html` + 暗色适配 | |
| 39 | 窗口尺寸/几何 | ✅ 900×600 恢复几何 | ⚠️ 1100×750，几何不保存 | UI 按新设计，尺寸可自定 |

## 汇总

- **已实现且可用（12）**：1-2, 4, 6-9, 11, 16-17, 20, 25, 32(历史/设置), 34-38
- **已实现但损坏 / 行为差异（8）**：3 拖放、10/21 事件字段、12 缓存键、13 暂停、14 批量取消、18 快速比较、22 复制格式
- **完全缺失 / 未接线（8）**：19 批量文件夹、23 导出、24 导入验证、26 历史写入、28-30 配置持久化、32 记事本
- **待确认**：get_batch_statistics 命令未注册（BUG-003，批量统计接口）
